name: Mercurio Keep-Alive

on:
  schedule:
    # Cada 10 minutos. GitHub puede retrasar la ejecución bajo carga alta,
    # pero el margen es suficiente antes de que Render duerma el servicio
    # (15 min sin tráfico).
    - cron: "*/10 * * * *"
  workflow_dispatch: {} # permite dispararlo manualmente para probar

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend /health
        run: curl -sf https://mercurio-9haf.onrender.com/health || curl -sf https://mercurio-9haf.onrender.com/ || exit 0
      - name: Ping keep-alive fallback
        run: curl -sf https://mercurio-9haf.onrender.com/health || echo "health check failed but not failing job"
